int a;
int b;
int c,d;

funcao(int k, float l, bool mu) -> int {}

fun(int i, bool j) -> int {
    funcao(a, 1.0, true);
    //f();
    a = 2+2;
    //a = 2+2.0;
    //f();
    //int g, y, _ble <= 2.0; 
    int g, y, _ble <= 2;
    //int g;  // nao deveria dar
    j = 2;  // nao deveria dar
    f();
    i = 1;
    c = a;
    if(b) {
        c = a;
    }
    else {
        c = a;
    };
}

//int c;

main () -> int {
    f(a, b, c);
    int _a, _b, _c;
    int g, h, i <= 0;
    {
        fun(i);
    };
    j(2+2, 2*2, 100);
    int a <= 2, b <= 1, c <= 3;
    if(a<2) {
        a = b + c;
    };

    /*teste*/
  
    while(c>0 & i < 3) {
        c = c - 1;
        i = i + 1;
        bool meme <= true;
        bool mene <= false;
        if(meme & mene) {
            c = 10000;
        };
    };
  
    /*teste1*/
    /*teste2*/

    if(a + j(2, 4, 4) > 5) { a = 5; }
    else { };

    if((a == 5) & ((2*2+3)*10 > 10)) {
        return 0;
    }
    else {
        return a;
    };
}
